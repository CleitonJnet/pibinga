<div class="{{ $col }}">
    <label class="w-full text-sm" for="{{ $id }}">{{ $name }}</label>
    <textarea rows="5" class="w-full p-2 text-sm rounded-md" id="{{ $id }}" name="{{ $id }}" placeholder="{{ $name }}"></textarea>
</div>
