<div class="{{ $col }}">
    <label class="w-full text-sm" for="{{ $id }}">{{ $name }}</label>
    <input class="w-full p-2 text-sm rounded-md" type="text" id="{{ $id }}" name="{{ $id }}" placeholder="{{ $name }}">
</div>
